package main

<<<<<<< HEAD
func average(xs []float64) (avg float64) { |\longremark{可以使用命名返回值；}|
	sum := 0.0
	switch len(xs) {
	case 0:                 |\longremark{如果长度是零，返回~0；}|
		avg = 0
	default:                |\longremark{否则，计算平均值；}|
		for _, v := range xs {
			sum += v
		}
		avg = sum / float64(len(xs)) |\longremark{为了使除法能正常计算，必须将值转换为~\key{float64}；}|
	}
	return  |\longremark{得到平均值，返回它}|
=======
func average(xs []float64) (avg float64) { |\longremark{We use a named return parameter;}|
	sum := 0.0
	switch len(xs) {
	case 0:                 |\longremark{If the length is zero, we return 0;}|
		avg = 0
	default:                |\longremark{Otherwise we calculate the average;}|
		for _, v := range xs {
			sum += v
		}
		avg = sum / float64(len(xs)) |\longremark{We have to % 
convert the value to a \key{float64} to make the division work;}|
	}
	return  |\longremark{We have an avarage, return it.}|
>>>>>>> 9d3c4dbbba2dd6ce9e8c33dbfcbd0d7ca7842eb7
}
